# The assets commit the E2E checks out and builds. Moved by assets CI's bump
# automation together with <assets.version> in pom.xml, whose .g<sha> suffix
# names this same commit — the e2e asserts the two agree.
#
# One file per producer ON PURPOSE: see producers/match.lock.
aabee7a4b07264db4e9da421383c2a9ffba3c65e